## Валидация и очистка данных

### Валидация данных для таблицы clients
[SQL-запросы](sql/02_data_quality_clients.sql)

**Выводы после валидации:**

| Что проверял | Что нашел | Какое решение принял | Обоснование принятия решения |
|---|---|---|---|
| Количество нулевых значений в столбцах | Пропуски только в `first_redeem_date`: 35 469 | Пока просто посмотрели на данные | – |
| Некорректно указанный возраст (вне 14–95) | 1 791 строка, 0,45% от общего числа значений| Строку с некорректным возрастом не удаляем, просто делаем ее `NULL` | Остальные поля клиента валидны, нет смысла удалять строку |
| Списание баллов раньше получения карты | 536 строк | Исключаем из воронки активации | Будет неправильно использовать такие строки при подсчете воронки активации |
| Карта выдана, но баллы ни разу не списывались | 35 469 строк, 8,86% от общего числа значений| Приняли как факт | Бизнес-факт|
| Насколько часто встречается `gender = 'U'` | 185 706 строк, 46,41% от общего числа значений | Не трогаем | Валидное значение. Скорее всего не получилось идентифицировать пол |

### Валидация данных для таблицы products
[SQL-запросы](sql/02_data_quality_products.sql)

**Выводы после валидации:**

| Что проверял | Что нашел | Какое решение принял | Обоснование принятия решения |
|---|---|---|---|
| Количество нулевых значений в столбцах | `level_1-4`: по 3; `segment_id`: 1 572; `brand_id`: 5 200; `vendor_id`: 34; `netto`: 3 | Пока просто посмотрел на данные | – |
| Выбросы `netto > 20` | 16 строк | Строки не удаляем, оставляем как данность | Выбросы концентрируются в нескольких общих категориях, а не распределены случайно по всему каталогу |

### Валидация данных для таблицы purchases
[SQL-запросы](sql/02_data_quality_purchases.sql)

**Выводы после валидации:**

| Что проверял | Что нашл | Какое решение принял | Обоснование принятия решения |
|---|---|---|---|
| Количество нулевых значений в столбцах | Пропуски только в `trn_sum_from_red`: 42 743 212 (93,4%) | Пока просто посмотрел на данные | - |
| Количество записей с неположительной суммой покупок | 620 строк; 0,0014 % от общего числа значений | Исключаются в будущих подсчетах денежных метрик | Были проведеный две проверки: 1) оплаты баллами не было 2) строки распределены по 576 клиентам. Учитывая эти проверки, мы можем предположить, что это были промо-товары с нулевой стоимостью.  |
| Диапазон дат транзакций | от 2018-11-21 до 2019-03-18 | Проверил, что диапазон совпадает с описанием датасета | - |
| Логическая связь между таблицами | Каждая строка из таблицы purchases ссылается на реально существующего клиента и реально существующий товар | - | - |

### Подготовка витрин для анализа
[SQL-запросы (VIEW внутри файлов выше)](sql/)

Для анализа метрик, нельзя было использвать сырые данные, поэтому поверх таблиц были созданы два view: `clients_clean` и `products_clean`.
Все дальнейшие запросы обращаются к этим витринам, а не к сырым таблицам напрямую.


